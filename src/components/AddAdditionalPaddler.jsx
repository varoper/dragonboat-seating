// AddAdditionalPaddler.js
// Allows adding a paddler that is not in the roster
import React, { useState, useEffect, useRef } from 'react';
import { storeExtraFrontWeight, storeExtraBackWeight, storeSteeringWeight } from '../utils/StorageHelpers';
import useStore from '../store/useStore';

const AddAdditionalPaddler = () => {



  return (

  );
};

export default AddAdditionalPaddler;
